#include<stdio.h>
main(){
	int length,weight,rectangle;
	printf("\n enter the value of length: ");
	scanf("%d",&length);
	printf("\n enter the vale of weigth:");
	scanf("%d",&weight);
	rectangle=length*weight;
	printf("area of ractangle=%d",rectangle);
	
}
